---

apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: "{{ params.name }}-{{ env }}"
  labels:
    {%- if params.labels %}
    {{ params.labels | to_yaml | indent(width=4) }}
    {%- endif %}
    acm-app: "{{ app }}"
spec:
  selector:
    matchLabels:
      acm-app: "{{ app }}"
