---

apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: "myapp-test-dev"
  labels:
    tenant: operating-platform
    acm-app: "myapp"
spec:
  selector:
    matchLabels:
      acm-app: "myapp"
